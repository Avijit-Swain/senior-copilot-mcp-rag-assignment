# Python dependencies. Install into a virtualenv:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# Corpus generation (scripts/generate_corpus.py)
reportlab==4.4.5

# PDF text extraction for the RAG ingestion pipeline
pdfplumber==0.11.10

# Embeddings and answer synthesis
openai==2.53.0
python-dotenv==1.2.2

# Vector store (embedded, persists to rag/index/)
chromadb==1.5.9

# Agent orchestration
langgraph==1.2.10
langchain-core==1.5.3

# Alarm Management API simulator
aiohttp==3.14.3

# Test runner
pytest==9.0.2
pytest-asyncio==1.3.0

# Model Context Protocol server
mcp>=1.13.0
